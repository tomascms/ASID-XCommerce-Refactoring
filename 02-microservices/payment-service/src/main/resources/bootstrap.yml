spring:
  cloud:
    config:
      enabled: false
      uri: http://localhost:8888/config
      fail-fast: false
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 6
